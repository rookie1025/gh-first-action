name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: |
          echo "hello world"
          pwd
          which echo
      - name: printgoodbye
        run: echo "done.. bye"
      - name: third stage
        run: |
          echo "testing the limits"
